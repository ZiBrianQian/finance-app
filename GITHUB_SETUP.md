# 📦 Настройка GitHub для автообновлений

## 1. Создание репозитория на GitHub

1. Откройте [github.com/new](https://github.com/new)
2. Введите имя репозитория (например: `finance-app`)
3. Выберите **Private** (приватный) или **Public** (публичный)
4. Нажмите **Create repository**

---

## 2. Настройка приложения

После создания репозитория нужно обновить конфигурацию:

### Файл: `electron/updater.js`
Замените строки 14-15:
```javascript
const GITHUB_OWNER = 'ваш-username-на-github';  // например: 'johndoe'
const GITHUB_REPO = 'finance-app';               // имя репозитория
```

### Файл: `electron-builder.json`
Замените строки 37-38:
```json
"owner": "ваш-username-на-github",
"repo": "finance-app",
```

---

## 3. Инициализация Git и push

Откройте терминал в папке `FinanceApp` и выполните:

```bash
# Инициализация git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Добавление удалённого репозитория (замените URL на свой)
git remote add origin https://github.com/YOUR_USERNAME/finance-app.git

# Push в main ветку
git branch -M main
git push -u origin main
```

---

## 4. Создание релиза (обновления)

### Автоматически (рекомендуется):
```bash
npm run electron:build
```
Затем вручную создайте Release на GitHub и загрузите файл `FinanceManager-Portable.exe`

### Вручную:
1. Соберите приложение: `npm run electron:build`
2. На GitHub перейдите: **Releases → Create a new release**
3. Tag version: `v1.0.1` (следующая версия)
4. Release title: `v1.0.1`
5. Загрузите файл `release/FinanceManager-Portable.exe`
6. Нажмите **Publish release**

---

## 5. Как работают обновления

```
Пользователь запускает приложение v1.0.0
        ↓
Приложение проверяет GitHub Releases
        ↓
Находит v1.0.1 → показывает уведомление
        ↓
Пользователь нажимает "Скачать"
        ↓
Файл скачивается в папку updates/
        ↓
Пользователь нажимает "Установить"
        ↓
Приложение создаёт update.bat и закрывается
        ↓
Батник заменяет старый .exe на новый
        ↓
Запускается новая версия v1.0.1
```

---

## ⚠️ Важно: Версия в package.json

При каждом релизе не забывайте **увеличивать версию** в `package.json`:

```json
{
  "version": "1.0.1"  // было 1.0.0
}
```

Тег на GitHub должен совпадать: `v1.0.1`
